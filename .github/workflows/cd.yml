name: cd

on:
  push:
    tags:
      - v*

jobs:
  publish:
    name: publish
    runs-on: ubuntu-24.04
    timeout-minutes: 3
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2

      - name: Restore Bun cache
        uses: actions/cache@v5
        with:
          key: bun-${{ hashFiles('bun.lock') }}
          restore-keys: bun
          path: |
            ~/.bun/bin
            ~/.bun/install
            node_modules

      - name: Install dependencies
        run: bun i && npm install -g npm@latest

      - name: Publish to npm
        run: bun run publish
